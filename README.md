# E-commerce App (Frontend + Backend)

A beginner-friendly full-stack eCommerce web app built using **React** and **Node.js**.  \
This project includes both **frontend** and **backend** components and serves as a complete learning starter for real-world development.

---

## 🛒 Overview

This repository contains a minimal but complete e-commerce starter:

- **backend/** → Node.js + Express + Mongoose (MongoDB). Auth with JWT.  \
- **frontend/** → React app (Create-React-App-like structure) using `fetch` to call backend APIs.

---

## ✨ Features Implemented

- **Users**: Signup / Login with JWT authentication (User/Admin roles).  \
- **Products**: Product model. Admin can create, update, and delete; users can view.
- **Cart**: Add or remove items to cart (stored in user document).  \
- **Orders**: Checkout saves order in the MongoDB database.  \
- **Admin Page**: Simple admin page in frontend to manage products.  \

---

## ⚙️ Quick Start (Local Setup)

### 🔹 Backend Setup
1. Install Node.js (v16 or higher).
2. Navigate to the backend folder:
   ```bash
   cd backend
